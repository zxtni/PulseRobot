# ⚡ PulseRobot

**PulseRobot** — a sleek **Vite + React + TypeScript** starter built for fast, component-driven UI with **Tailwind CSS** and **Radix primitives**.  
Designed for devs who love clean structure, speed, and scalability.

---

## 🚀 Quick Start

### Prerequisites
- Node.js 18+
- npm / pnpm / yarn

### Setup

```bash
npm install
npm run dev
```

### Build & Preview
```bash
npm run build
npm run preview
```

### Lint
```bash
npm run lint
```

---

## 🧩 Project Structure
```
PulseRobot/
├─ src/
│  ├─ components/      # UI + sections
│  ├─ components/ui/   # primitives (Button, Dialog, etc.)
│  ├─ pages/           # page views
│  ├─ main.tsx         # app entry
│  └─ App.tsx          # root component
├─ public/             # static assets
└─ index.html          # Vite entry
```

---

## 🛠️ Git Setup

If not connected yet:
```bash
git init
git add .
git commit -m "chore: initial commit"
git branch -M main
git remote add origin https://github.com/zxtni/PulseRobot.git
git push -u origin main
```

---

## 🌐 Deploy Options

- **Vercel** — auto deploy from `main`  
- **Netlify** — build with `npm run build` → output `dist/`  
- **GitHub Pages** — deploy `dist/` via `gh-pages` or Actions  

*(Want me to add a ready GitHub Actions workflow for Pages? I can drop it in.)*

---

## 📜 License
MIT © [zxtni](https://github.com/zxtni)

---

**Made with ❤️ + TypeScript by Rahul Mondal**
